import { ShippingPriceModel } from './shipping-price-model';

describe('ShippingPriceModel', () => {
  it('should create an instance', () => {
    expect(new ShippingPriceModel()).toBeTruthy();
  });
});
